name: Create Release and Update Version

on:
  workflow_dispatch:

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          persist-credentials: false # otherwise, the token used is the GITHUB_TOKEN, instead of your personal access token.
          fetch-depth: 0 # otherwise, there would be errors pushing refs to the destination repository.

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '16.x'

      - name: Parse commit message
        id: commit_message
        run: echo "commit_message=$(git log --format=%B -n 1 HEAD)" >> $GITHUB_ENV

      - name: Update version
        id: update_version
        run: |
          git config user.name "ivan"
          git config user.email "ivan@users.noreply.github.com"
          COMMIT_MSG=$(git log --format=%B -n 1 ${{ github.sha }})
          echo $COMMIT_MSG
          case "$COMMIT_MSG" in
            *BREAKING\ CHANGE:*) npm version major ;;
            *feat*) npm version minor ;;
            *) npm version patch ;;
          esac
          echo "new_version=$(node -p -e "require('./package.json').version")" >> $GITHUB_ENV

      - name: Create tag
        id: tag
        run: |
          git tag -a "${{ env.new_version }}" -m "${{ env.commit_message }}"

      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.ACCESS_TOKEN }}
          branch: ${{ github.ref }}

      - name: Create release
        uses: actions/create-release@v1
        with:
          tag_name: '${{ env.new_version }}'
          release_name: '${{ env.new_version }}'
          body: '${{ env.commit_message }}'
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
